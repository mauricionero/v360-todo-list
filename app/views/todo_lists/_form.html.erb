<%= form_with(model: todo_list) do |form| %>
  <% if todo_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo_list.errors.count, "error") %> prohibited this todo_list from being saved:</h2>

      <ul>
        <% todo_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, maxlength: 30, size: 20 %>
    <%= form.color_field :color %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, maxlength: 255, size: 50 %>
  </div>

  <div class="field">
    <%= form.label :limit_date %>
    <%= form.datetime_select :limit_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
